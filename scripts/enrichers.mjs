/**
 * Enrichers management for the module.
 */
export class Enrichers {
    /**
     * Setup the enrichers on module startup.
     */
    static setup() {
        console.log("Enrichers set up.");
    }
}